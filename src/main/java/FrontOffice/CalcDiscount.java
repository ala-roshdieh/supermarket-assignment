package FrontOffice;

import BackOffice.Product;

import java.util.ArrayList;

/**
 * This class takes in a list, iterates through it, and calculates a discount based
 * on what it finds in the list.
 */
public class CalcDiscount extends Checkout{

    protected double calculateTotal(ArrayList<Product> productList) {

        return 0;
    }
}
